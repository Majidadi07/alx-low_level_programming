#include "main.h"

/**
* main - tests function that prints if integer is positive or negative
* @void: take no input to main
* Return: provide 0 error for a succesful run
*/

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
