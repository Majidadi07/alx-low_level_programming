#include <stdio.h>
#include <stdlib.h>

/**
 * main - Prinmary entry or starting point
 *
 * Return: give 0 for success
 *
 */

int main(void)
{
	char lc;

	for (lc = 'a'; lc <= 'z'; lc++)
	{
		putchar(lc);
	}
		putchar('\n');
	return (0);
}
