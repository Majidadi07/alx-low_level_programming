#include <stdio.h>
#include <stdlib.h>

/**
 * main - Starting function
 *
 * Return: provie 0 error for success run
 *
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");

	return (0);
}
